<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Emilio Cabrera">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    <title>{% block title %}Odontología Integral{{ app.user ? app.user.username : ''}}{% endblock %}</title>

    <!-- Bootstrap core CSS -->
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('css/base.css') }}">
        <link rel="stylesheet" href="{{ asset('vendor/font-awesome/css/font-awesome.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/nav.css') }}">
    {% endblock %}
    {% block specific_stylesheets %}{% endblock specific_stylesheets %}
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>

<body>
{% block body %}
    <!-- NAVBAR
    ================================================== -->
    {% block navbar %}
        <div class="navbar-wrapper">
            <div class="container">
                <img class = "logo0" src="{{asset('/Public/image/NINOS1.jpg')}}"  >
                <img class = "logo1" src="{{asset('/Public/image/Logo0.jpg')}}"  >
                <nav class="navbar navbar-inverse navbar-static-top">
                    <div class="container">
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                                <span class="sr-only">Toggle navigation</span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>
                            <a class="navbar-brand" href="#">Inicio</a>
                        </div>
                        <div id="navbar" class="navbar-collapse collapse">
                            <ul class="nav navbar-nav">
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Especialidades <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        <li><a href="#">Ortodoncia</a></li>
                                        <li><a href="#">Odontopetria</a></li>
                                        <li><a href="#">Endodoncia</a></li>
                                        <li><a href="#">Maxilofacial</a></li>

                                    </ul>
                                </li>
                                <li><a href="#contact">Contacto</a></li>
                                <li><a href="#contact">¿Quiénes somos?</a></li>
                            </ul>
                            <ul class="nav navbar-nav navbar-right">
                                {% if is_granted('ROLE_USER') %}
                                    <li><p class="navbar-text text-muted"><span class="fa fa-user"></span> {{ app.user }}</p></li>
                                    <li><a href="{{ path('security_logout') }}">Logout <span class="fa fa-lock"></span></a></li>
                                {% else %}
                                    <li><a href="{{ path('security_login') }}">Login <span class="fa fa-key"></span></a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    {% endblock navbar %}

    {% block flashMessages %}
        {% if app.session.flashBag.has('danger') or app.session.flashBag.has('warning') or app.session.flashBag.has('success') or app.session.flashBag.has('info') %}
            <div class="container main-content">
                {% for msg in app.session.flashBag.has('danger') %}
                    <div class="row">
                        <div class="col-xs-12">
                            <div class="alert alert-danger">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true" onclick="this.parentNode.style.display='none'">&times;</button>
                                <strong>Error!</strong> {{ msg }}
                            </div>
                        </div>
                    </div>
                {% endfor %}
                {% for msg in app.session.flashBag.get('warning') %}
                    <div class="row">
                        <div class="col-xs-12">
                            <div class="alert alert-warning">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true" onclick="this.parentNode.style.display='none'">&times;</button>
                                <strong>Cuidado!</strong> {{ msg }}
                            </div>
                        </div>
                    </div>
                {% endfor %}
                {% for msg in app.session.flashBag.get('success') %}
                    <div class="row">
                        <div class="col-xs-12">
                            <div class="alert alert-success">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true" onclick="this.parentNode.style.display='none'">&times;</button>
                                <strong>Éxito!</strong> {{ msg }}
                            </div>
                        </div>
                    </div>
                {% endfor %}
                {% for msg in app.session.flashBag.get('info') %}
                    <div class="row">
                        <div class="col-xs-12">
                            <div class="alert alert-info">
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true" onclick="this.parentNode.style.display='none'">&times;</button>
                                <strong>Info!</strong> {{ msg }}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    {% endblock flashMessages %}

    {% block content %}

    {% endblock content %}
    
    <!-- FOOTER -->
    {% block footer %}
        <footer class="container" style = "margin-top: 10%">
            <p class="pull-right"><a href="#">Back to top</a></p>
            <p>&copy; IngenioWeb.&middot; <a href="#">Privacidad</a> &middot; <a href="#">Terminos</a></p>
        </footer>
    {% endblock footer %}

{% endblock body %}
{% block javascripts %}
    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    {% block jquery %}<script src="{{ asset('vendor/jquery/dist/jquery.slim.min.js') }}"></script>{% endblock jquery %}
    <script src="{{ asset('vendor/bootstrap/collapse.js') }}"></script>
    <script src="{{ asset('vendor/bootstrap/dropdown.js') }}"></script>
{% endblock javascripts %}
{% block specific_javascripts %}{% endblock specific_javascripts %}
</body>
</html>
